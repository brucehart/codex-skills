interface:
  display_name: "GH Commit and Create PR"
  short_description: "Commit changes on a feature branch and open a PR"
  default_prompt: "Use $gh-commit-and-create-pr to commit my current changes on a new feature branch and open a PR back into the current branch unless I specify another base."
